/**
 * FaceAI Dataset Module (Placeholder)
 *
 * Responsibility:
 * - Store captured face images locally (IndexedDB or backend)
 * - Manage person IDs and metadata
 *
 * Planned Public API:
 * - FaceAI.dataset.save(label, imageBlob, metadata)
 * - FaceAI.dataset.getAll()
 * - FaceAI.dataset.remove(personId)
 *
 * TODO: Implement when building dataset for recognition
 */
"use strict";

FaceAI.dataset = {
  save() {
    /* TODO */
  },
  getAll() {
    /* TODO */
  },
  remove() {
    /* TODO */
  },
};
